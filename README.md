# Without-Internet-Chat-Over-LAN-
This is a without internet chat over LAN project. 
Without Internet Chat Over LAN is a real-time communication that allows multiple users to communicate over a Local Area Network (LAN) without an internet connection. Built using Node.js, Express, Socket.IO, and MongoDB, it supports user authentication, private messaging, and separate chat history for each user.
